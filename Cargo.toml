[package]
name = "webinterface-onboot"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
local-ip-address = "0.5.0"

[profile.release]
opt-level = 's'
lto = true
codegen-units = 1
